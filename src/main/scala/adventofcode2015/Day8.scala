package adventofcode2015

import scala.io.Source

object Day8 extends App {
//  def calculateCharDiff(input: List[String]): Int = {
//    val stringLiteralSize = input.map(_.length).sum
//
//    }

  // TODO: 1. how to represent escaped hex correctly in scala\java, 2. how to treat escape sequences as literal chars
  val input = Source.fromResource("day8Input.txt").getLines.toList
}
